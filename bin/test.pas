program test02;

	var
		x, y, z: integer;
		a, b, c: real;
		t, u, v: boolean;

	function factorial(n : integer): integer;
		begin
			if n > 1 then
				factorial := n * factorial(n-1)
			else
				while n > 0 do
					repeat
						begin
							n := n-1;
							a := 2/3;
						end
					until t = true;
				factorial := 1;
		end;

	begin
	end.
